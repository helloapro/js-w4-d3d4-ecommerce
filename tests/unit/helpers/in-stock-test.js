
import { inStock } from 'ecommerce/helpers/in-stock';
import { module, test } from 'qunit';

module('Unit | Helper | in stock');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = inStock([42]);
  assert.ok(result);
});

