
import { inStockButton } from 'ecommerce/helpers/in-stock-button';
import { module, test } from 'qunit';

module('Unit | Helper | in stock button');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = inStockButton([42]);
  assert.ok(result);
});

